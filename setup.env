# Photo Heatmap Viewer environment variables

# Path to your photo libraries
PHOTOS_ROOT_DIR=./photos

# Libraries to process in format: folder1:name1,folder2:name2
# Example: "vacation:Vacation Photos,family:Family Album"
LIBRARIES=library1:Library1,library2:Library2

# Schedule for library updates (cron format)
# Default: At minute 0 of every 6th hour (0 */6 * * *)
UPDATE_SCHEDULE=0 */6 * * *

# Timezone for proper timestamp handling
TZ=UTC
